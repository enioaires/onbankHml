import { Animated } from 'react-native';
import styled from 'styled-components/native';

export const ModalBody = styled(Animated.View)`
    padding: 28px;
    padding-bottom: 60px;
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    border-radius: 10px;
    background-color: #f8f8f8;
`;
